language: node_js

sudo: false

branches:
  only:
    - master
    - develop

node_js:
  - '8'

install:
  - npm i -g codecov typescript
  - npm i

script:
  - npm run build

after_success:
  - npm run test

stages:
 - deploy

jobs:
  include:
    - stage: deploy
      deploy:
        provider: npm
        email: "${MY_EMAIL}"
        api_key: "${NPM_TOKEN}"
        skip_cleanup: true
        on:
          branch: master